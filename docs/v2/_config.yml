theme: jekyll-theme-cayman

title: Psi-Continuum v2 Documentation
description: >
  Psi-Continuum v2 — archived documentation for a minimal
  one-parameter phenomenological extension of ΛCDM
  with deformation parameter ε₀.

# --- Project metadata ---
project_name: Psi-Continuum
project_version: v2 (archived)
software_version: 0.2.3
status: archived

# --- Markdown settings ---
markdown: kramdown
kramdown:
  input: GFM
  hard_wrap: false

highlighter: rouge

# --- Links & paths ---
baseurl: "/docs/v2"
relative_links:
  enabled: true
  collections: true

# --- Assets ---
include:
  - assets/custom.css

# --- Build behaviour ---
exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
